require 'spec_helper'

describe HerokuBuilder do
  it 'has a version number' do
    expect(HerokuBuilder::VERSION).not_to be nil
  end

  # it 'does something useful' do
  #   expect(false).to eq(true)
  # end
end
